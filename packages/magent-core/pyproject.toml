[project]
name = "magent-core"
version = "0.1.0.dev"
description = ""
authors = [{ name = "brokun", email = "brokun0128@gmail.com" }]

dependencies = ["pydantic>=2.7.3"]
readme = "README.md"
requires-python = ">= 3.9"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true

dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/magent_core"]
