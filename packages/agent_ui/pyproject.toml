[project]
name = "agent_ui"
version = "0.1.0"
description = ""
authors = [
  { name = "shiyu", email = "dongyuxin.dyx@antgroup.com" },
  { name = "brokun", email = "brokun0128@gmail.com" },
]

dependencies = []
readme = "README.md"
requires-python = ">= 3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.rye]
managed = true

dev-dependencies = []

[tool.hatch.metadata]
allow-direct-references = true

[tool.hatch.build.targets.wheel]
packages = ["src/agent_ui"]
